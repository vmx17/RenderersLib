<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Renderers">
    
    <Style TargetType="local:BoxRenderer" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BoxRenderer">
                    <SwapChainPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="{TemplateBinding Param0}" Height="{TemplateBinding Param1}" Background="{TemplateBinding Background}">
                        <TextBlock>This is the SwapChainPanel in NuGet package.</TextBlock>
                    </SwapChainPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
